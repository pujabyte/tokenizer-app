# src/ — Source

Scoped guide for the source tree. Inherits the repo root [`../AGENTS.md`](../AGENTS.md).

_Document this layer's structure and conventions: main modules/directories, the canonical pattern
for adding a unit, boundaries between layers, and where tests live._

- Follow the root conventions and `harness/rules/`.
- Keep units small and focused; clean interfaces; test touched code to ≥95% coverage.
- Match the existing layout; no unrequested refactors.
